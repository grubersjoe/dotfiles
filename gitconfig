[alias]
    a  = add
    b  = branch
    c  = commit
    co = checkout
    d  = diff
    dc = diff --cached
    s  = status
    pu  = pull
    cc = ! git rev-parse --short=8 HEAD | tr -d '\n' | pbcopy
    pf = push --force-with-lease

[core]
    autocrlf = input
    excludesfile = ~/.gitignore
    pager = delta

[credential]
    helper = cache

[diff]
    colorMoved = default

[include]
    path = ~/.gitconfig.local

[init]
    defaultBranch = main

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictstyle = diff3

[push]
    default = current
	autoSetupRemote = true

[pull]
    rebase = true

[rebase]
    autoStash = true

[user]
    name = Jonathan Gruber

[include]
    path = /Users/jonathan.gruber/git/foss/delta/themes.gitconfig

[delta]
    navigate = true
    side-by-side = true

[tig "color"]
    cursor = black magenta
    title-blur = black blue
    title-focus = black blue
